/* import { cartList, quantityList } from "../main"

export const checkId = () => {
    for (let i = 0; i < cartList.length; i++){
        if(cartList[i].id == cartList[i].id){
        quantityList.push (cartList[i].quantity);
        console.log(quantityList);
        }
    }

}
 */
